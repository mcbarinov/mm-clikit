# Copy to .env and edit. .env is gitignored.

# Used by `just sync-docs` to propagate docs/cli-architecture.md
# to consuming CLI projects. Space-separated absolute paths.
# Leading ~/ is expanded to $HOME.
CLI_PROJECTS="~/projects/foo ~/projects/bar"
